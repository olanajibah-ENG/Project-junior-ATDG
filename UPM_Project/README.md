# خدمة إدارة المشاريع (UPM Project)

خدمة Django متخصصة في إدارة مشاريع البرمجة وربط الملفات بالمشاريع.

## 🏗️ هيكل المشروع

```
UPM_Project/
├── 📄 README.md                    # دليل هذا المشروع
├── 🐳 Dockerfile                  # حاوية Docker
├── 📄 entrypoint.sh              # سكريبت بدء الخدمة
├── 📄 requirements.txt           # متطلبات Python
├── 📄 manage.py                  # أداة إدارة Django
├── 📄 FRONTEND_API_GUIDE.md      # دليل API للواجهة الأمامية
├── 📄 DOCKER_README.md           # إرشادات Docker
├── 📁 UPM_Project/               # إعدادات Django الرئيسية
│   ├── settings.py              # إعدادات المشروع
│   ├── urls.py                  # مسارات URL الرئيسية
│   └── wsgi.py                  # إعدادات WSGI
└── 📁 core_upm/                  # التطبيق الأساسي للمشاريع
    ├── models/                  # نماذج البيانات
    │   ├── project.py           # نموذج المشروع
    │   ├── artifact.py          # نموذج الملف
    │   └── user.py              # نموذج المستخدم
    ├── views/                   # API endpoints
    │   ├── project_views.py     # إدارة المشاريع
    │   ├── artifact_views.py    # إدارة الملفات
    │   └── user_views.py        # إدارة المستخدمين
    ├── serializers/             # تسلسل البيانات
    ├── business_logic/          # منطق العمل
    │   ├── project_service.py   # خدمات المشاريع
    │   ├── artifact_service.py  # خدمات الملفات
    │   └── user_service.py      # خدمات المستخدمين
    ├── repositories/            # طبقة الوصول للبيانات
    ├── external_storage.py      # واجهة التخزين الخارجي
    └── urls.py                  # مسارات API
```

## 🚀 الميزات الرئيسية

### إدارة المشاريع
- **إنشاء المشاريع**: إضافة مشاريع برمجة جديدة
- **إدارة الملفات**: ربط ملفات الكود بالمشاريع
- **التحكم في الصلاحيات**: كل مستخدم يرى مشاريعه فقط
- **REST API كامل**: واجهة برمجية شاملة

### ربط مع خدمة التحليل
- **إرسال للتحليل**: إمكانية إرسال ملفات للتحليل الآلي
- **تخزين النتائج**: حفظ نتائج التحليل في قاعدة البيانات
- **تكامل كامل**: عمليات متزامنة مع خدمة AI

## 🔧 الإعداد والتشغيل

### متطلبات النظام
- Python 3.10+
- MySQL 8.0+
- Docker (اختياري)

### خطوات التشغيل

1. **تثبيت المتطلبات**
   ```bash
   pip install -r requirements.txt
   ```

2. **إعداد قاعدة البيانات**
   ```bash
   # إنشاء قاعدة بيانات MySQL
   CREATE DATABASE upm_mysql_db;
   CREATE USER 'admin3'@'%' IDENTIFIED BY 'admin3';
   GRANT ALL PRIVILEGES ON upm_mysql_db.* TO 'admin3'@'%';
   ```

3. **تشغيل الخدمة**
   ```bash
   # تشغيل migrations
   python manage.py migrate

   # تشغيل الخادم
   python manage.py runserver 0.0.0.0:8000
   ```

## 📡 API Endpoints

### إدارة المستخدمين
```
POST   /api/upm/signup/              # إنشاء حساب جديد
POST   /api/upm/login/               # تسجيل الدخول
```

### إدارة المشاريع
```
GET    /api/upm/projects/            # سرد مشاريع المستخدم
POST   /api/upm/projects/            # إنشاء مشروع جديد
GET    /api/upm/projects/{id}/       # تفاصيل مشروع محدد
PUT    /api/upm/projects/{id}/       # تحديث مشروع
DELETE /api/upm/projects/{id}/       # حذف مشروع
```

### إدارة الملفات
```
GET    /api/upm/projects/{id}/artifacts/     # ملفات المشروع
POST   /api/upm/projects/{id}/artifacts/     # إضافة ملف للمشروع
GET    /api/upm/artifacts/{id}/              # تفاصيل ملف
PUT    /api/upm/artifacts/{id}/              # تحديث ملف
DELETE /api/upm/artifacts/{id}/              # حذف ملف
```

### المصادقة
```
POST   /api/token/                  # الحصول على token
POST   /api/token/refresh/          # تجديد token
POST   /api/token/verify/           # التحقق من token
```

## 🔍 أمثلة الاستخدام

### إنشاء مشروع جديد
```bash
curl -X POST http://localhost:8001/api/upm/projects/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "project_name": "My Python Project",
    "project_description": "A sample Python project"
  }'
```

### إضافة ملف للمشروع
```bash
curl -X POST http://localhost:8001/api/upm/projects/123/artifacts/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file_name=main.py" \
  -F "code_language=python" \
  -F "code_content=print('Hello World')" \
  -F "code_version=1.0"
```

## 🗄️ قاعدة البيانات

### جداول MySQL
- **projects**: معلومات المشاريع
- **codeartifacts**: ملفات الكود المرتبطة بالمشاريع
- **users**: بيانات المستخدمين

### ربط مع MongoDB
- الخدمة تتكامل مع خدمة AI لتحليل الملفات
- نتائج التحليل تُحفظ في MongoDB

## 🧪 الاختبارات

### تشغيل الاختبارات
```bash
# من المجلد الجذر للمشروع
python tests/simple_final_test.py
```

## 🔧 استكشاف الأخطاء

### مشاكل شائعة

1. **مشاكل قاعدة البيانات**
   ```bash
   # فحص إعدادات قاعدة البيانات في settings.py
   python manage.py dbshell
   ```

2. **مشاكل الصلاحيات**
   ```
   # تأكد من إرسال Authorization header
   curl -H "Authorization: Bearer YOUR_TOKEN" ...
   ```

3. **مشاكل الاتصال بخدمة AI**
   ```
   # فحص إعدادات AI_SERVICE_URL في settings.py
   ```

## 📚 المراجع

- [Django Documentation](https://docs.djangoproject.com/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [MySQL Documentation](https://dev.mysql.com/doc/)

## 👥 المساهمون

- [اسم المطور]

---

**ملاحظة**: هذه الخدمة تتكامل مع خدمة AI لتحليل الكود وخدمة Nginx كبوابة API.