# نظام توليد التوثيق التلقائي بالذكاء الاصطناعي

## 📋 نظرة عامة
نظام متقدم لتحليل الكود وتوليد التوثيق التقني تلقائياً باستخدام الذكاء الاصطناعي. يدعم لغات Python و Java ويولد تقارير احترافية بتنسيق PDF و Markdown.

## 🚀 الميزات الرئيسية

### 1. تحليل الكود الذكي
- **تحليل AST متعدد اللغات**: Python (AST مدمج) و Java (Tree-sitter)
- **استخراج الهيكل**: Classes, Methods, Functions, Dependencies
- **كشف أنماط التصميم**: Factory, Singleton, Observer, Strategy, Adapter
- **تحليل العلاقات**: Composition, Inheritance, Association

### 2. نظام الوكلاء الثلاثي
- **High-Level Agent**: شروحات تنفيذية للإدارة (بدون تفاصيل تقنية)
- **Low-Level Agent**: توثيق تقني مفصل للمطورين
- **Verifier Agent**: مراجعة جودة لضمان دقة الشرح

### 3. مولد المخططات التلقائي
- **Class Diagrams**: مخططات UML تلقائية من الكود
- **علاقات التركيب**: كشف `self.engine = Engine()` تلقائياً
- **تصدير Mermaid**: دمج المخططات في التقارير

### 4. تصدير احترافي
- **PDF مصمم**: تصميم بنفسجي احترافي مع ترقيم الصفحات
- **Markdown للمطورين**: تنسيق نظيف للـ README
- **دمج المخططات**: Class Diagrams مدمجة في التقارير

### 5. نظام التخزين المؤقت
- **توفير التكلفة**: تجنب إعادة توليد الشروحات الموجودة
- **سرعة الاستجابة**: من ثوانٍ إلى ميلي ثانية
- **ذكي**: يدعم البحث بالمعرف الأصلي والموحد

## 🛠️ التقنيات المستخدمة

### Backend
- **Django 4.2.14**: إطار العمل الرئيسي
- **MongoDB**: قاعدة بيانات للتحليلات والشروحات
- **Celery + Redis**: معالجة غير متزامنة
- **Tree-sitter**: تحليل كود Java
- **Python AST**: تحليل كود Python

### AI & NLP
- **OpenRouter API**: واجهة الوصول للذكاء الاصطناعي
- **Google Gemini 2.0 Flash**: نموذج الذكاء الاصطناعي المجاني
- **نظام إعادة المحاولة**: Exponential backoff للـ rate limiting

### Export & Documentation
- **xhtml2pdf**: توليد PDF احترافي
- **Markdown**: تنسيق للمطورين
- **Mermaid.js**: مخططات تفاعلية

## 📁 هيكل المشروع

```
Ai_project/
├── core_ai/
│   ├── ai_engine/
│   │   ├── agents.py          # الوكلاء الثلاثة
│   │   ├── orchestrator.py    # منسق العمليات
│   │   ├── llm_client.py      # عميل الذكاء الاصطناعي
│   │   └── doc/
│   │       ├── pdf.py         # مولد PDF
│   │       └── markdown.py    # مولد Markdown
│   ├── language_processors/
│   │   ├── base_processor.py  # واجهة أساسية
│   │   ├── python_processor.py # معالج Python
│   │   └── java_processor.py   # معالج Java
│   ├── models/
│   │   └── codefile.py        # نماذج البيانات
│   ├── views/
│   │   ├── export_views.py    # عمليات التصدير
│   │   └── analysis.py        # عمليات التحليل
│   └── admin.py               # واجهة الإدارة
└── requirements.txt           # المتطلبات
```

## 🔧 التثبيت والتشغيل

### 1. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 2. إعداد قاعدة البيانات
```bash
python manage.py migrate
```

### 3. إعداد متغيرات البيئة
```bash
# في ملف .env
OPENROUTER_API_KEY=your_api_key_here
MONGODB_URI=mongodb://localhost:27017/ai_project
```

### 4. تشغيل الخادم
```bash
python manage.py runserver
```

### 5. تشغيل Celery (للمعالجة غير المتزامنة)
```bash
celery -A Ai_project worker --loglevel=info
```

## 📚 استخدام النظام

### 1. رفع ملف الكود
```python
POST /api/codefiles/
{
    "filename": "example.py",
    "file_type": "python",
    "content": "class Car:\n    def __init__(self):\n        pass"
}
```

### 2. طلب التحليل
```python
GET /api/analysis-results/{file_id}/
```

### 3. توليد الشرح
```python
GET /api/ai-explanations/{analysis_id}/?type=high_level
GET /api/ai-explanations/{analysis_id}/?type=low_level
```

### 4. تصدير التقرير
```python
POST /api/export/
{
    "explanation_id": "60f7b1c8e4b0c8a2d8f9e1a2",
    "format": "pdf",
    "include_diagram": true
}
```

## 🎯 حالات الاستخدام

### للمطورين
- **مراجعة الكود**: فهم كود الزملاء بسرعة
- **التوثيق التلقائي**: توليد README و documentation
- **تحليل المشاريع**: فهم هيكل المشاريع الكبيرة

### للمدراء التقنيين
- **تقارير تنفيذية**: ملخصات غير تقنية للمشاريع
- **تقييم الجودة**: مراجعة أنماط التصميم المستخدمة
- **التخطيط**: فهم dependencies بين المكونات

### للفرق
- **Knowledge Transfer**: نقل المعرفة بين أعضاء الفريق
- **Code Review**: مراجعة منهجية للكود
- **Documentation**: توثيق موحد ومتسق

## 🔍 أمثلة عملية

### مثال: تحليل كلاس Python
```python
# الكود المدخل
class Car:
    def __init__(self, model, horsepower):
        self.model = model
        self.engine = Engine(horsepower)
    
    def drive(self):
        return self.engine.start()

# النتيجة: Class Diagram
{
    "classes": [
        {
            "name": "Car",
            "methods": ["__init__", "drive"],
            "associations": [
                {
                    "target_class": "Engine",
                    "type": "Composition",
                    "attribute": "engine"
                }
            ]
        }
    ]
}
```

### مثال: شرح High-Level
```markdown
## Executive Summary
هذا الكود يمثل نموذج سيارة في نظام محاكاة أو لعبة، حيث تحتوي كل سيارة على محرك وتستطيع القيادة.

## Purpose & Responsibility
الهدف الرئيسي هو تمثيل كائن السيارة مع إمكانية التحكم في المحرك والقيادة.

## Key Capabilities
- إنشاء سيارة جديدة مع تحديد الموديل وقوة المحرك
- تشغيل المحرك والقيادة
- ربط السيارة بمحرك محدد
```

### مثال: شرح Low-Level
```markdown
## Technical Implementation

### Class: Car
**Constructor (__init__)**
- يستقبل معاملين: model (نوع string) و horsepower (نوع numeric)
- ينشئ خاصية self.model لحفظ موديل السيارة
- ينشئ كائن Engine جديد ويمرر له horsepower
- يحفظ المحرك في self.engine (علاقة Composition)

**Method: drive()**
- يستدعي دالة start() من كائن المحرك
- يعيد النتيجة مباشرة
- لا يحتوي على معالجة أخطاء
```

## 🚀 التطوير المستقبلي

### ميزات مخططة
- **دعم لغات إضافية**: JavaScript, C++, C#
- **تحليل الأداء**: كشف bottlenecks في الكود
- **تحليل الأمان**: كشف الثغرات الأمنية المحتملة
- **تكامل Git**: تحليل تاريخ التغييرات
- **واجهة ويب**: لوحة تحكم تفاعلية

### تحسينات تقنية
- **Vector Database**: للبحث الدلالي في الكود
- **RAG System**: مقارنة مع توثيق موجود
- **Real-time Analysis**: تحليل فوري أثناء الكتابة
- **Team Collaboration**: مشاركة التحليلات بين الفريق

## 📞 الدعم والمساهمة

### الإبلاغ عن المشاكل
- استخدم GitHub Issues للمشاكل التقنية
- تأكد من تضمين logs والخطوات للإعادة

### المساهمة
1. Fork المشروع
2. إنشاء branch جديد للميزة
3. Commit التغييرات مع رسائل واضحة
4. إنشاء Pull Request

### الاتصال
- Email: [your-email@domain.com]
- GitHub: [your-github-username]
- Documentation: [link-to-detailed-docs]

---
**تم إنشاء هذا التوثيق تلقائياً بواسطة نظام AI Documentation System**